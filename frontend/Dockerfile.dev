# Local development image. Production still uses frontend/Dockerfile.
FROM oven/bun:1

WORKDIR /app

COPY package.json bun.lock /app/
COPY frontend/package.json /app/frontend/

WORKDIR /app/frontend

RUN bun install

EXPOSE 5173

CMD ["bun", "run", "dev", "--host", "0.0.0.0"]
